{
  "name": "lab-projects-page",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve ."
  },
  "dependencies": {
    "glob": "^10.0.0",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
